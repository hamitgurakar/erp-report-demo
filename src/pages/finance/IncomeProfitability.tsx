import { FinancePlaceholder, type FinancePageProps } from './_Placeholder';

export const IncomeProfitability = ({ t, l, lang }: FinancePageProps) => (
  <FinancePlaceholder t={t} l={l} lang={lang} title={l.mhFin0} />
);
