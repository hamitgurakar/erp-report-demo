import { FinancePlaceholder, type FinancePageProps } from './_Placeholder';

export const Valuation = ({ t, l, lang }: FinancePageProps) => (
  <FinancePlaceholder t={t} l={l} lang={lang} title={l.mhFin6} />
);
