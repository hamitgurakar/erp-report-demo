import { FinancePlaceholder, type FinancePageProps } from './_Placeholder';

export const Leverage = ({ t, l, lang }: FinancePageProps) => (
  <FinancePlaceholder t={t} l={l} lang={lang} title={l.mhFin5} />
);
