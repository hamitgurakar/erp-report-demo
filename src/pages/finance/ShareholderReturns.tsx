import { FinancePlaceholder, type FinancePageProps } from './_Placeholder';

export const ShareholderReturns = ({ t, l, lang }: FinancePageProps) => (
  <FinancePlaceholder t={t} l={l} lang={lang} title={l.mhFin7} />
);
